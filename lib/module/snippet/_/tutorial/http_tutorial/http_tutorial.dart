import 'package:flutter/material.dart';

class HttpTutorialView extends StatelessWidget {
  const HttpTutorialView({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold();
  }
}
